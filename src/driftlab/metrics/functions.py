"""
Implements pure functions to calculate metrics (e.g., embeddings via sentence-transformers, simple text matching) on run outputs.
"""

def compute_metric(output: str, expected: str = None) -> float:
    pass
